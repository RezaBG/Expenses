import React from "react";

const Product = (props) => {
  return (
    <div>
      <p>Product titel: {props.title}</p>
      <p> Product price: {props.title}</p>
    </div>
  );
};

export default Product;
